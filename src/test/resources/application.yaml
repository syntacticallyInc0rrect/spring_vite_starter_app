server:
  port: 8080
  servlet:
    session:
      cookie:
        secure: true
        http-only: true
      tracking-modes: COOKIE
spring:
  application:
    name: midterm
  datasource:
    url: jdbc:mysql://localhost:3306/sample_test_db
    username: user
    password: password
  jpa:
    database: mysql
  flyway:
    baseline-on-migrate: true
